# Copy this file to `.streamlit/secrets.toml` for LOCAL use.
# Never commit the real secrets.toml to GitHub.
#
# For Streamlit Community Cloud, paste these values into:
# App -> Settings -> Secrets
# amnsEAy2008
# deployment.mode values:
#   local       = normal local use
#   cloud_test  = HTTPS/camera testing only; local SQLite/photos may disappear
#   production  = intentionally blocked in v0.9 until persistent cloud storage exists

[deployment]
mode = "local"

[app_auth]
enabled = true

# Generate each password_hash by running:
#   .\.venv\Scripts\python.exe generate_password_hash.py

[users.parkingadmin]
display_name = "Church Administrator"
role = "admin"
password_hash = "$2b$12$EpNVGnseeb0Cbbfz44wTiu4iwJdztYlMftztjlRun1tFf9ClhFc0m"

[users.volunteer1]
display_name = "Volunteer 1"
role = "volunteer"
password_hash = "$2b$12$VOLUNTEER1_HASH"

[users.volunteer2]
display_name = "Volunteer 2"
role = "volunteer"
password_hash = "$2b$12$VOLUNTEER2_HASH"

[users.volunteer3]
display_name = "Volunteer 3"
role = "volunteer"
password_hash = "$2b$12$VOLUNTEER3_HASH"

[supabase]
url = "https://emchhgqbfificsnirkjq.supabase.co"
secret_key = "YOUR_sb_secret_KEY"
bucket = "violation-photos"
